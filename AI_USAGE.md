# AI 使用说明

> 本作业开发由 Claude Code（转接 DeepSeek V4 Pro）完成代码实现，用户负责方案拍板与验收。
> 本文回答题目必问的三件事：用了哪些 AI 工具怎么拆任务、AI 在哪里出错怎么发现怎么修、哪些部分用户决定自己写及理由。

## 一、用了哪些 AI 工具，怎么拆任务

**用了两个 AI**：

| AI | 用途 |
|---|---|
| Claude Code（开发 AI，转接 deepseek-v4-pro） | 写全部代码、测试、文档；按小步清单推进 |
| DeepSeek V4 Pro（产品内 AI，function calling） | 看板里的"AI 数据问答"：自然语言 → 工具调用 → 真查库 → 真实数字回答 |

**怎么拆任务**：整体拆 8 步，每步先列小步清单、做完一小步验证再做下一小步；每完成一个功能立即跑对应测试，全绿立即 git commit，一个里程碑一个提交。

```
Step 1 骨架+数据副本 → Step 2 数据清洗管线(27测试) → Step 3 看板API(15测试)
→ Step 4 AI链路(mock 8 + live 6 测试) → Step 5 前端看板+聊天 → Step 6 文档
→ Step 7 部署上线 → Step 8 缓冲打磨
```

**真实 prompt 例子（开发 AI 收到的任务指令，用户原话，节选）**：

> "我提一嘴营业额异常和周末的关系，显然周末和周末比，工作日和工作日比，且工作日往往比周末冷清，
> 明显违背这个的才算异常。出现异常确可以点出来，我之前说不好，主要是你在图中标的有点乱，
> 让人感觉意图不清晰。可以直接以文字形式给出异常当月异常情况和原因等。"

→ AI 据此把异常算法从"与全体日均比"升级为"同星期对比+留一法"，并把图上标点移除、改为文字原因展示（提交 74c20bd）。

**产品内 AI 的真实 prompt（系统提示词全文）见第三节**——用户提问时只发自然语言，铁律由后端注入。

## 二、产品内 AI 的接入架构

```
用户提问 ──▶ api/chat.py（SSE 流式）
                │ 系统提示词 = 数据元信息（日期边界/门店名/品类）+ 9 条铁律
                ▼
        DeepSeek V4 Pro（思考模式开）── 产出 tool_calls（如 query_metrics）
                ▼
        ai/tools.py 白名单工具 ──▶ query.py 唯一取数层 ──▶ SQLite（白名单 SQL + 参数化绑定）
                ▼
        工具结果（只含聚合数字）回喂 LLM ──▶ 流式回答
```

**agent 循环不用框架**（LangChain 等一概不用，用户拍板手写，理由见第六节）：`chat.py` 里一个 for 循环（最多 3 轮）——模型出工具调用 → 白名单执行 → 结果回喂 → 循环 → 最终回答。

关键设计：
- **LLM 不直接碰 SQL 和原始数据**：它只产出结构化参数，SQL 由后端白名单拼装；工具层只返回聚合结果，全链路没有返回原始行的工具。
- **数字一致性由架构保证**：AI 工具与看板 API 共用 `query.py` 同一套查询函数 → AI 回答的数字 = 看板数字。
- **思考模式**：DeepSeek V4 Pro 的 reasoning_content 经 `thinking` 事件流式透出到前端（可折叠展示），带工具调用的消息必须回传 reasoning_content。

## 三、真实系统提示词（全文见 backend/app/api/chat.py build_system，此处为铁律部分）

```
你是连锁餐饮公司「POKE ONE」的经营数据分析助手，用中文回答。

数据范围：2026-05-01 ~ 2026-07-31，已清洗入库 11815 条销售流水。
门店：Super Souper(拉面/上海·徐汇)、Makai Poke(轻食/...)、……（只注入名称/品类/区，无行级数据）

铁律：
1. 任何涉及数字的问题，必须先调用工具查数据库；回答中的数字只能来自工具返回结果，
   禁止凭记忆或推测编造。
2. 工具查不到（无匹配/空结果）→ 如实说「数据里没有」，可建议换个问法；
   绝不编造数字、名称或事实，拿不准就明说拿不准。
3. 日期换算：把「六月/上个月/最近30天」等自然语言转成 YYYY-MM-DD 闭区间。
4. 门店/商品名用用户原话传给工具参数，由后端模糊匹配。
5. 追问时结合对话历史理解指代（如「那五月呢」= 上一个问题的同口径换 5 月）。
6. 回答简洁：先给结论数字，再一句解读；金额单位为元。
7. 回答中的数字必须原样取自工具返回结果；如需对比可计算差额/环比，
   但必须同时给出计算所用的两个原始数字。
8. 涨跌方向结论必须明确用「上涨」或「下跌」表述。
9. 名称必须原样使用数据库中的原始名称：门店名是英文（如 Super Souper），
   绝不翻译成中文；商品名是中文（如 牛肉poke），绝不翻译成英文；店名与商品名不许混用或改写。
```

## 四、AI 在哪里出了错，怎么发现、怎么修的

**关于"编造数字"**：产品内 AI 全程由 live 测试守护，从未编造过经营数字——问"北京烤鸭六月卖了多少钱"如实回答"数据里没有"（test_live_fallback_no_fabrication 实测）。开发 AI 出的错主要是算术错误、口径错误、实现错误，全部被测试或用户验收抓出，无一个流入交付。

| # | 发现者 | 现象 | 根因 | 怎么修 |
|---|---|---|---|---|
| 1 | **测试发现（周末误报）** | 异常预警 6 个"异常日"全是周末（05-02 周六、06-07 周日…），把周末正常火爆误报为异常 | z-score 与全体日均比：工作日低值拉低均值，周末系统性高出 2.5σ；算法未建模星期周期性 | 升级为**同星期对比+留一法**（周末和周末比、工作日和工作日比），异常改为文字原因呈现；由定向单元测试锁定行为（6 周夹具、跨星期零误报断言） |
| 2 | 测试抓出 | 定向测试报不出异常 | 留一法索引用错：全序列下标当成星期桶下标，异常日排除不掉自己 → 基线被稀释 | 每个日期记录其在星期桶内的位置（pos_of）再留一 |
| 3 | 测试抓出 | mock 测试下工具参数被静默清空（KeyError） | openai SDK 流式 tool_calls 是 pydantic 对象，`.get()` 改 `getattr` "双兼容"是半吊子——dict 用 getattr 恒返 None | 先 isinstance 归一化成 dict 再处理；教训：两种形态要么归一化要么显式双路径 |
| 4 | 测试抓出 | AI 把测试期望值算错两次（14−6=9；漏数订单） | LLM 算术错误 | 单元测试断言与手工对账 → 修正。**这正是"测试抓 AI 算术错误"的价值** |
| 5 | 测试抓出 | 追问"那五月呢"断上下文 | done 事件只回传原始历史+最终回答，中间工具轮丢了 | 回传完整 msgs（含 tool_calls+工具结果），前端全量回传 |
| 6 | 用户 | 无流式输出、经营建议按钮无反馈 | 主链路第一轮用非流式 create；等待期零反馈 | 改真流式 + thinking 透出 + 前端思考动画/已提问回显 |
| 7 | 用户 | "全部"区间 Top10 商品为空 | 拼 URL 缺 `?` → 404 被前端吞掉 | `${q ? q+'&' : '?'}limit=10` |
| 8 | AI（早期自查） | 通读大 CSV 导致死机半小时 | 把 574KB 数据整读进上下文 | 立铁律：只读前 3 行验格式，一切处理走脚本看聚合 |

## 五、自动化测试怎么守护"数字真实"

1. **mock 一致性**（test_ai_consistency.py）：mock LLM 出工具调用、工具真执行 → 断言「回答中每个数字 ⊆ 工具结果数字集」+ 兜底不编造 + 追问历史回传。
2. **live 对账**（test_ai_live.py）：真 key 打真模型，三样例问题断言回答数字 ⊆ 工具结果数字 ∪ 可回推验证的运算结果（差额/和/百分比），并与库内锚点对账（如牛肉poke 六月 13,440.0 元）。live 测试还抓出断言口径问题：必须区分"日期元信息"（提示词注入的数据范围）与"编造数字"。
3. **架构兜底**：AI 拿不到原始行数据（工具只返回聚合），数字源头唯一（query.py），看板与 AI 天然对账一致。

## 六、哪些部分用户决定自己写，为什么

| 部分 | 谁决定/写 | 为什么 |
|---|---|---|
| **AI 问答的 agent 循环（不用框架）** | **用户决定手写** | 不用 LangChain 等 Agent 框架：一个 for 循环（最多 3 轮）+ 白名单工具执行，约 30 行代码完全可控——面试能逐行讲清，无黑盒依赖，出问题好排查。用框架反而要解释框架行为 |
| 异常算法口径 | 用户拍板 | "周末和周末比、工作日和工作日比，明显违背的才算异常"+ 文字呈现（第二节真实 prompt 原文） |
| 技术栈与部署形态 | 用户拍板 | FastAPI+SQLite+React+ECharts、单容器部署（3 小时窗口最少翻车点） |
| UI 取舍 | 用户拍板 | 移除趋势图红点标记（粒度错配不直观）、异常改文字卡片 |
| 全部代码实现/测试/文档 | Claude Code 编写 | 按"一次一小步、每功能一测、每里程碑一提交"纪律执行，用户逐功能验收 |
| 验收与 bug 发现 | 用户 | 浏览器验收 7 条反馈（含发现周末误报 bug）、清洗规则确认、最终上线拍板 |

**协作铁律**（CLAUDE.md）：重要决策先讨论后动工；未测试不提交；bug 台账实时记录（发现者/现象/根因/修复）；数据内容绝不进 AI 上下文（四层隔离）。

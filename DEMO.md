# 演示说明（DEMO）

> 面试演示脚本：每一步「怎么做 → 会看到什么 → 数字为什么可信」。
> 全部数字来自真实数据库查询，与看板接口同源（唯一取数层 query.py），可信证明见文末。

## 0. 演示准备（2 分钟）

```bash
cd backend && python -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt && cp .env.example .env           # 填入 DEEPSEEK_API_KEY
uvicorn app.main:app --port 8000                                   # 自动清洗 data/*.csv 建库
cd ../frontend && npm install && npm run build
# 浏览器打开 http://localhost:8000
```

数据概况（启动后 /api/health 可见）：2026-05-01 ~ 2026-07-31，5 家门店，清洗后 11,815 条流水。

## 1. 第一关：看板（接口数字与直接 SQL 对账）

| 演示动作 | 会看到 | 可信证明 |
|---|---|---|
| 打开首页 | 深色主题看板：KPI 卡（全量营业额 421,377.0 元 / 11,814 单 / 客单价 35.67 元）+ 趋势图 + Top10 商品 | `curl http://localhost:8000/api/dashboard/summary` 与页面数字一字不差；`backend/tests/test_dashboard.py` 15 个测试把接口数字与手工期望对账 |
| 切"本月"（2026-07） | KPI、趋势图、Top10、门店对比全部联动切换 | 所有图表共用同一区间参数，数字来自同一取数层 |
| 看 Top10 商品 | 牛肉poke 全量 39,774 元居首（六月 13,440 元） | `curl "/api/dashboard/top-products"` 同源数字 |
| 选单店 S01 Super Souper | 全板过滤到该店 | store_id 过滤同样参数化下推 SQL |

## 2. 第二关：AI 问答（三样例问题 + 兜底 + 追问）

### 样例 1：哪个品类的门店营业额最高？

- 问：**"在全部数据范围内，哪个品类的门店营业额最高？"**
- 答（真模型实测）：**日料，87,816.0 元**（并列出各品类排名）
- 可信证明：看板接口 `GET /api/dashboard/categories` 的 store_categories[0] = 日料 87,816.0——AI 与看板**同源同数字**；live 测试 `test_live_store_category_top` 自动断言该数字与库内 Top1 一致

### 样例 2：牛肉poke 六月卖了多少钱？

- 问：**"牛肉poke 六月卖了多少钱？"**
- 答（真模型实测）：**13,440.0 元**
- 可信证明：库内锚点（六月商品维度牛肉poke = 13,440.0 元）；live 测试 `test_live_product_june` 断言回答含该数字、且工具确实按 2026-06 查询

### 样例 3：客单价最近是涨了还是跌了？

- 问：**"客单价最近是涨了还是跌了？"**
- 答（真模型实测）：**上涨**——模型自查 5/6/7 三个月客单价 36.43 / 34.93 / 35.64，结论"7 月 35.64 比 6 月 34.93 上涨"
- 可信证明：live 测试 `test_live_aov_direction` 用库内 7 月 vs 6 月客单价断言方向词

### 兜底：数据里没有的，绝不编造

- 问：**"北京烤鸭六月卖了多少钱？"**
- 答：**"数据里没有北京烤鸭"**，并建议换个问法——零编造（live 测试 `test_live_fallback_no_fabrication` 守护）

### 追问上下文

- 接着问：**"那五月呢？"** → 自动改查 5 月牛肉poke：**13,104.0 元**，还能引用上一轮的 6 月数字算出差额 336 元（live 测试 `test_live_followup_may` 验证）

## 3. 第三关进阶演示

| 演示点 | 怎么演示 |
|---|---|
| 流式输出 | 提问后正文逐字流出，上方虚线框显示"🤔 正在思考…"（可点开看模型思考过程） |
| 图表联动 | AI 问"六月"时，左侧看板自动跳到 6 月区间（事件总线联动，无需手动切换） |
| AI 数字自动化测试 | `cd backend && pytest tests/test_ai_live.py -v`——真 key 打真模型，6 个对账断言现场全绿 |
| 门店对比视图 | 左侧门店对比图（营业额柱 + 客单价线），切换区间即时联动 |
| 部署上线 | Docker Compose 单命令上线（见 README/部署章节） |

## 4. 加分项演示

**① 手写深色 UI（有审美）**：无组件库，纯手写 CSS 深色主题；卡片化布局、涨跌色（红涨绿跌徽标）、响应式网格。

**② 异常销售预警（同星期对比）**：
- 算法：每家店每天只与**同为该星期几**的历史均值/σ 比（周末和周末比、工作日和工作日比），留一法排除当天自身，|z|≥2.5 且基准样本≥2 才判异常
- 演示：右侧"异常预警"卡片以文字给出异常与原因，例如
  - `2026-05-05（周二）Super Tetsudo ▼偏低 ¥335，较同周二日均 -57.4%（z=-2.86）`——工作日和工作日比出来的真实低谷
  - `2026-06-07（周日）Makai Poke ▲偏高 ¥1,974，较同周日日均 +62.0%（z=3.57）`——同日 3 家店齐涨（Juicy Bao Bao +60.5%、Super Tetsudo +42.5%），是真实的周日爆发（如节假日），而不是"周末都这样"
- 对照：旧版"与全体日均比"会把 6 个周末日全部误报（周末天然火爆）；新算法全量 20 个异常日分布在工作日和周末，每个都有可解释的方向与幅度

**③ AI 主动经营建议**：点异常卡片下方"✨ 让 AI 基于当前数据给经营建议 →"，AI 会真实查询当前区间的门店/品类/异常数据，给出带数字的建议（思考与工具执行过程全程可见）。

## 5. 可信证明汇总

1. **架构同源**：AI 工具与看板接口共用唯一取数层 query.py → 数字一致性由架构保证，无需人工对账
2. **自动化测试 56 个**：
   - 清洗 27：8 类脏数据规则逐一断言（审计等式 12,131 = 11,815 + 78 + 238）
   - 看板 15：接口数字与手工期望/直接 SQL 对账 + 异常算法定向测试 + 非法参数 400
   - AI mock 8：「回答中每个数字 ⊆ 工具结果数字集」+ 兜底 + 追问（mock LLM、工具真执行）
   - AI live 6：真 key 打真模型，三样例问题与库内锚点对账（无 key 自动 skip）
3. **数据隔离四层**：LLM 只能经白名单工具拿聚合结果，全链路不存在返回原始行的工具；系统提示词只注入元信息（日期边界/门店名/品类）
4. **开发可审计**：DEVLOG.md 全程记录每步做什么/测什么/错在哪；git 阶梯状提交历史（feat/fix/docs/test 前缀 + 中文描述）
